Skeleton stands in for content whose layout you already know.

```jsx
<Skeleton variant="media" ratio="16 / 9" />
<Skeleton lines={3} />
```

- Match the real element's size or the page will jump when content arrives.
- Under 400ms show nothing at all.
