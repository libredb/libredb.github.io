Badge marks state or category. It is never clickable — use Tag for that.

```jsx
<Badge tone="success" dot>Sağlıklı</Badge>
<Badge tone="brand">Yeni</Badge>
```

- Status tones must carry real meaning; never use them decoratively.
- `brand` uses the gradient and is reserved for one "new/featured" marker per view.
