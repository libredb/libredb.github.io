---
import StubPage from '../layouts/StubPage.astro';
import ProductSection from '../components/home/ProductSection.astro';
---

<StubPage
  path="/playground"
  title="Playground"
  description="Open a real LibreDB Studio against a seeded demo database — the SQL editor, the grid, ER diagrams and the health dashboard, with nothing to install."
  eyebrow="Playground"
  heading="Try it against a real database, in a browser tab"
  intro="The playground is a live Studio wired to a seeded demo instance. Run queries, open the ER diagram, read the health panel — the same build you would deploy next to your own data."
  cta={{ label: 'Open live demo', href: 'https://app.libredb.org' }}
  secondary={{ label: 'Deploy your own', href: '/#deploy' }}
>
  <ProductSection />
</StubPage>
