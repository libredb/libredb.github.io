---
interface Props {
  title: string;
  subtitle?: string;
}
const { title, subtitle } = Astro.props;
---
<header class="mb-8">
  <h2 class="text-2xl font-bold tracking-tight text-bright sm:text-3xl">{title}</h2>
  {subtitle && <p class="mt-2 text-[14.5px] text-dim">{subtitle}</p>}
</header>
