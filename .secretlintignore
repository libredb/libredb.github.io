dist
.astro
node_modules
bun.lock
cms/.next

# Example env file for the local Outstatic dashboard. The values are documented
# placeholders (`your-github-oauth-...`), not real credentials.
cms/.env.local.example

# This post's subject IS the shape of a MongoDB connection URI, so it prints the
# template `mongodb://<user>:<password>@<host>:<port>/<database>`. The rule matches
# the URI shape, not the values, so every placeholder style trips it equally
# (`<user>`, `USER`, `{user}` were all tried) and there is no inline disable in
# this preset. Scoped to the one file rather than to the posts directory, so a
# real credential pasted into any other post is still caught.
outstatic/content/posts/mongodb-authsource-connection-failure.md
